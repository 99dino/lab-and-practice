#include <stdio.h>
int main()
{
    int a = 5, s = 0, n;
    scanf("%d", &n);
    for (int i = i = 0; i < n; i++)
    {
        s += a;
        a += 2;
    }
    printf("Sum of series upto N terms = %d", s);
    return 0;
}